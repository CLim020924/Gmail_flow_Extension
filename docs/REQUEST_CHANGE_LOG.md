# 사용자 요청 및 기능 변화 기록

이 문서는 CMOE Workspace와 Gmail Flow의 사용자 요청, 해석, 실제 기능 변화, 검증 결과를 한 흐름으로 연결한다. Git 커밋은 코드의 변경점을 보존하고, 이 문서는 **왜 바뀌었는지와 어떤 요청에서 시작되었는지**를 보존한다.

개인정보가 포함된 명단·이메일·전화번호 예시는 원문을 복제하지 않고 데이터 형식만 기록한다. OAuth 비밀키, 토큰, 계정 비밀번호도 기록하지 않는다.

## 기록 규칙

앞으로 기능 요청을 처리할 때 다음을 함께 갱신한다.

1. 요청마다 `REQ-연월일-번호`를 부여한다.
2. 사용자의 요청 핵심 문장을 가능한 그대로 남긴다.
3. 기존 동작, 변경 이유, 최종 동작을 연결한다.
4. 구현 파일과 검증 방법을 적는다.
5. 완료 커밋 해시를 적고, 미완료이면 `설계`, `진행 중`, `보류`로 표시한다.
6. 기존 기능을 다시 바꾸면 이전 요청 ID를 `이전 단계`에 연결한다.

## 요청에서 현재 기능까지의 흐름

### Gmail Flow 기반 기능

| 요청 ID | 사용자 요청 핵심 | 이전 단계 → 최종 동작 | 상태·근거 |
|---|---|---|---|
| REQ-20260731-01 | Gmail Flow를 외부 Windows PC에서 프로그램 형식으로 사용하고 싶다. | Chrome 확장 전용 → Electron 기반 Windows 휴대형 앱 | 완료 · `bcc7a08` |
| REQ-20260731-02 | Google 승인 후 `client_secret is missing` 오류 없이 로그인하고 싶다. | 확장 OAuth 방식 → 데스크톱 OAuth/PKCE 및 암호화된 토큰 저장 | 완료 · `c7de467`, `6682143`, `bcc7a08` |
| REQ-20260731-03 | 프로그램에서 뜨지 않는 이름 입력창 등 모든 입력 대화상자를 구현해달라. | 브라우저 기본 입력창 의존 → 앱 내부 입력·확인 대화상자 | 완료 · 관련 UI 커밋군 |
| REQ-20260731-04 | 명단/구조/표 데이터/Excel CSV 불러오기를 단순하게 묶고 저장 명단을 관리하고 싶다. | 흩어진 저장·불러오기 버튼 → 명단 관리 흐름과 저장 자료 라이브러리 | 완료 · `efc4e4e`, 이후 Workspace 통합 |
| REQ-20260731-05 | HWP 표처럼 헤더 없이 세로로 붙는 데이터도 패턴을 보고 자동 구분해달라. | 탭 표 중심 붙여넣기 → 이메일·전화번호·반복 패턴을 이용한 구조화 붙여넣기 | 완료 · `69a6894` |
| REQ-20260731-06 | 셀을 Excel처럼 드래그 선택하고 Ctrl+C/붙여넣기/삭제를 지원해달라. | 단일 입력 중심 → 범위 선택과 클립보드 셀 작업 | 완료 · `74e2c81` 및 명단 편집 후속 커밋 |
| REQ-20260731-07 | 이메일 형식 아이디와 실제 이메일 컬럼을 구분해 발송 차단 오류를 없애달라. | 이메일 모양 컬럼의 중복 판정 → 사용자가 지정한 이메일 역할을 우선하는 판정 | 완료 · `558f937`, `420f134` |
| REQ-20260731-08 | 메일 작성에서 여러 첨부파일을 선택하고 발송/초안에 넣고 싶다. | 첨부 없음 → 다중 첨부 선택과 MIME 메일 처리 | 완료 · Gmail Flow 데스크톱 기능군 |
| REQ-20260731-09 | EXE는 한 개만 실행되고 시작·처리·메일 대상별 진행도를 눈으로 보고 싶다. | 중복 실행 가능·무표시 처리 → 단일 인스턴스 잠금, 시작/큐/대상별 상태 표시 | 완료 · `0d38ced`, 데스크톱 main 기능군 |
| REQ-20260731-10 | 설치형과 휴대형을 모두 제공하고 싶다. | 휴대형 단일 배포 → NSIS 설치형과 휴대형 동시 빌드 | 완료 · Workspace 배포 설정 |
| REQ-20260731-11 | 저장 자료를 내부에만 둘지 Google Drive로 외부 PC에서도 쓸지 사용자가 선택하게 해달라. | 로컬 저장 고정 → 로컬 저장 기본값과 선택형 Drive 동기화 구조 | 구현 기반 완료 · Workspace 저장/동기화 설정 |
| REQ-20260731-12 | Excel 셀을 Gmail에 붙일 때처럼 표·색·폰트·테두리를 메일 작성과 수정에서도 유지하고 싶다. | 일반 텍스트 본문 → 안전하게 정제된 HTML 표/서식 붙여넣기 | 완료 · Workspace Gmail 편집기 및 smoke 검사 |
| REQ-20260731-13 | 프로그램에서 만든 Gmail 초안을 일괄 수정하고 Gmail 직접 수정/삭제도 감지하고 싶다. | 생성만 가능 → 개인별 수정, 연결된 초안 갱신, 외부 변경 상태 표시 | 구현 완료 · Gmail 초안 아티팩트/상태 기능군 |
| REQ-20260827-01 | 오래된 파일을 정리하고 재설치한 뒤 시작 오류가 발생한다. | 읽을 수 없는 DPAPI 토큰에서 오류 출력 → 손상 토큰 자동 백업 후 재로그인 유도 | 완료 · `d5f6abe` |
| REQ-20260827-02 | 제목·본문에서 `{`를 입력하면 커서 바로 아래에 컬럼 목록이 떠야 한다. | 하단 변수 버튼 또는 일부 화면만 지원 → 독립 Gmail Flow와 Workspace 제목·본문·개별 수정창의 커서 자동완성 | 완료 · `4c17d3d`, `f350993` |

### Workspace 플랫폼과 모듈 구조

| 요청 ID | 사용자 요청 핵심 | 이전 단계 → 최종 동작 | 상태·근거 |
|---|---|---|---|
| REQ-20260819-01 | Gmail Flow와 별도로 확장 가능한 CMOE Workspace 플랫폼을 만들고 새 프로젝트/전환을 지원해달라. | 단일 Gmail 앱 → 설치 가능한 Workspace 플랫폼과 프로젝트 관리 | 완료 · `97ea842` |
| REQ-20260819-02 | Gmail Flow 같은 프로그램은 플랫폼 안에서 전역 설치하고, 프로젝트 안 또는 단독으로 실행하고 싶다. | 프로젝트 종속 기능 → 전역 모듈과 프로젝트/단독 실행 진입 구조 | 기반 완료 · Workspace 모듈 카탈로그/프로그램 창 |
| REQ-20260819-03 | 특정 프로그램만 바로 실행하는 바탕화면·시작 메뉴 바로가기를 만들고 전역 명단·계정을 공유하고 싶다. | Workspace를 먼저 열어야 함 → 프로그램별 독립 창과 공유 데이터 연결 | 기반 완료 · 프로그램 창/바로가기 API |
| REQ-20260819-04 | KAC, 교육, 안내, 문서 검토, Excel 등 업무가 바뀌어도 단계와 형식을 사용자가 구성·저장하게 해달라. | 고정 프로젝트 유형 → 순서·설명·체크리스트·설정을 바꾸는 버전형 업무 템플릿 | 완료 · `4270e0b` |
| REQ-20260819-05 | 예전 Gmail Flow의 UI와 기능을 빼거나 위치를 크게 바꾸지 말고 버튼을 단순·일관되게 유지해달라. | 새 플랫폼식 버튼 재배치 → 기존 화면 성격 유지, 자주 쓰는 동작 우선, 나머지는 메뉴로 묶음 | 계속 적용 중 · UI 후속 커밋군 |
| REQ-20260819-06 | 처음 사용하는 사람도 기능을 알 수 있도록 번역·이름·설명을 친숙하게 해달라. | 기술/모듈 중심 명칭 → 업무 목적 중심 한글 명칭과 단계 설명 | 적용 중 · Workspace 화면 문구/워크플로 UI |
| REQ-20260827-04 | 다른 컴퓨터에 설치했을 때 `Cannot find module 'exceljs'` 오류 없이 실행하고 싶다. | 개발 PC의 모듈 경로 의존 → ExcelJS 런타임 포함, 잠금 파일 기반 설치, 빌드 사전검사, 선택형 OAuth 로컬 파일 | 배포 검증 중 · `414f6a9`, `c5831f2` |

### 전역 명단과 Excel식 편집

| 요청 ID | 사용자 요청 핵심 | 이전 단계 → 최종 동작 | 상태·근거 |
|---|---|---|---|
| REQ-20260819-07 | 명단 가져오기를 누르면 Gmail Flow 명단 UI와 기능을 공통 창으로 띄우고 다른 명단 UI는 없애고 싶다. | 화면별 별도 명단 → 전역 명단 준비/관리 창 | 완료 · `38b472d`, `f28f5c5` |
| REQ-20260819-08 | 명단에서 드래그, 붙여넣기, 지우기와 프로젝트에 필요한 셀 작업을 지원해달라. | 제한적인 셀 입력 → Excel식 범위 작업과 행 자동 확장 | 완료 · `74e2c81`, `db59a70` |
| REQ-20260819-09 | 각 행에 임시 삭선 기능을 두고 제외자를 빼서 새 명단으로 저장하고 싶다. | 원본 행 삭제 → 원본 보존형 임시 제외와 선별 명단 저장 | 완료 · `d6c9da2` |
| REQ-20260819-10 | 삭선된 행은 삭제나 붙여넣기로 덮어쓰이지 않도록 고정해달라. | 제외 표시만 존재 → 제외 행 편집 보호 | 완료 · `c77768c` |
| REQ-20260819-11 | 단일 셀 편집이 다중 선택으로 오인되어 글자 하나 대신 셀 전체가 지워지는 문제를 고쳐달라. | 클릭 즉시 범위 선택 동작 → 셀 텍스트 편집과 범위 작업 분리 | 완료 · `74e2c81` |
| REQ-20260819-12 | 기본 5행을 유지하고 마지막 `+`로 계속 행을 추가하며 20행 이상 붙여넣으면 자동 확장되게 해달라. | 데이터 수만큼만 행 표시 → 최소 빈 행, 수동 추가, 붙여넣기 자동 확장 | 완료 · `db59a70` |
| REQ-20260819-13 | 프로젝트 저장 명단과 이름을 붙여 저장한 명단을 Gmail Flow 및 다른 기능이 공동 사용하게 해달라. | 각 프로그램의 독립 명단 → 프로젝트 명단과 공용 저장 명단 라이브러리 공유 | 완료 · `400e1a4`, `fdbbcda` |

### 일정·세션·Excel 출력

| 요청 ID | 사용자 요청 핵심 | 이전 단계 → 최종 동작 | 상태·근거 |
|---|---|---|---|
| REQ-20260819-14 | Google Forms 신청자/가능 시간 응답을 바탕으로 다양한 기간과 인원 구조의 일정을 편성하고 수정하고 싶다. | 1대1 고정 가정 → 역할·세션·가용 시간·규칙을 분리한 유동적 일정 모델 | 기반 완료 · `5b8be86` |
| REQ-20260819-15 | 1대1뿐 아니라 코치-다수, A/B반, 여러 세션과 단계별 통과 명단을 유동적으로 구성하고 싶다. | 코치/피코치 고정 → 사용자 정의 역할과 파생 명단, 세션별 대상 구성 | 기반 완료 · `5b8be86`, `d6c9da2` |
| REQ-20260819-16 | 자동 편성 결과를 만들어진 시트에서 바로 Excel처럼 수정하고 Excel 파일과 호환하고 싶다. | 결과 미리보기 중심 → 일정 시트 직접 편집, 붙여넣기, 동적 행·열 구조 | 구현 기반 완료 · 일정 편집 기능군 |
| REQ-20260819-17 | 일정 변경 시 사람을 드래그해 옮기고 빈 시간대에 놓으면 시간을 정하며 충돌을 확인하고 싶다. | 재생성 중심 → 세션별 수동 배치·변경 모델과 충돌 계산 | 구현 완료 · 세션 플래너/일정 보드 및 `REQ-20260827-03` |
| REQ-20260819-18 | 사용자가 Excel 출력 틀을 직접 바꾸고 기존 파일 형식에 맞춰 저장하고 싶다. | 고정 출력 → 동적 컬럼·레이아웃/내보내기 기반 | 일부 구현 · 자유 서식 편집기는 계속 개선 |
| REQ-20260819-19 | Google 계정과 Zoom 계정은 항상 같지 않으므로 기능별 계정을 세밀하게 선택하고 싶다. | 단일 계정 가정 → Gmail/Forms/Drive/Zoom 연결을 분리한 계정 설정 | 기반 완료 · Workspace 연결 관리 |
| REQ-20260827-03 | 한 고객의 일정 변경으로 다른 고객 일정도 재조정할 때 전체 캘린더에서 영향을 보고, 고객을 선택해 바꾼 일정이 전체 일정에 반영되게 해달라. UI는 기존 흐름처럼 복잡하지 않아야 한다. | 클릭·드롭 즉시 저장 → 고객 검색·현재 일정 선택·이동 후보 시각화·영향 미리보기·명시적 적용·전체 일정/메일/Zoom 상태 반영·실행 취소 | 구현 완료 · `8ebf327` |

### REQ-20260827-03 · 고객 중심 일정 예외 재조정

- 사용자 요청: “한 고객의 일정변경으로 인해 다른 고객 스케줄도 재조정해야 할 때 시각화하고, 고객을 선택해 일정 조정하면 전체 캘린더에 반영되게 해달라.”
- 이전 단계: `REQ-20260819-17`의 세션 카드 직접 배치. 고객·세션 클릭과 드롭이 곧바로 저장되어 변경 범위와 후속 영향을 적용 전에 확인하기 어려웠다.
- 결정: 기존 각진 패널·작은 상태 표시·Excel 일정표를 유지하고, 별도 복잡한 화면 대신 기존 일정 페이지 안에서 고객을 먼저 고르는 흐름을 추가했다.
- 최종 동작: 고객 검색 → 변경할 현재 일정 선택 → 전체 날짜의 이동 가능·확인 필요·차단 후보 확인 → 다른 고객/Zoom/메일 영향 미리보기 → 변경 적용 또는 취소. 직접 표 편집·가져오기·행 삭제도 같은 후속 확인 규칙을 사용한다. 실행 취소·다시 실행은 일정만 복원하고 실제 Gmail·Zoom 식별자와 최신 이력은 보존한 채 영향받은 결과물을 보수적으로 `다시 확인 필요`로 유지한다.
- 구현: `workspace/index.html`, `workspace/styles.css`, `workspace/app.js`, `workspace/operations-core.js`, `workspace/desktop/main.js`.
- 검증: 일정 변경 순수 함수 테스트, 전체 Workspace 단위 테스트 7종, 문법 검사, 데스크톱 smoke 시나리오 갱신, 데스크톱/좁은 화면 로컬 브라우저 검토와 적용·취소·Undo·Redo 상호작용 확인. 설치본 smoke 통과 여부는 `REQ-20260827-04`의 배포 검사에서 별도로 확인한다.
- 커밋: `8ebf327`

### REQ-20260827-04 · 다른 PC 설치본의 런타임 누락 방지

- 사용자 요청: “이렇게 떠 다른 컴퓨터에 설치하니까.” 설치본 시작 직후 Electron 메인 프로세스에서 `Cannot find module 'exceljs'`가 발생했다.
- 이전 단계: 개발 PC에 존재하는 ExcelJS 설치 경로에 의존했고, Git에서 제외한 `desktop/oauth-credentials.local.js`도 시작 시 무조건 읽었다. 따라서 개발 PC에서는 실행돼도 깨끗한 체크아웃이나 다른 PC의 설치본에서는 시작 실패가 날 수 있었다.
- 결정: 현재 `workspace/vendor` 구조를 유지하되 ExcelJS 전체 런타임을 설치 리소스로 복사하고, 빌드 직전에 설치 버전·잠금 파일·리소스 매핑을 검사한다. OAuth 로컬 파일은 선택 사항으로 바꾸고 설치 파일에서 제외한다.
- 최종 동작: 깨끗한 환경에서 `npm ci`가 `vendor/package-lock.json`에 고정된 ExcelJS를 설치한다. `build:installer`는 사전검사 실패 시 설치 파일 생성을 중단한다. Gmail Flow는 로컬 Client Secret 파일이 없어도 시작하고, 빈 Secret을 Google 토큰 요청에 보내지 않는다. 다음 설치본 버전은 `1.1.11`이다.
- 구현: `workspace/package.json`, `workspace/scripts/preflight-build.js`, `workspace/desktop/gmail-flow-host.js`, `desktop/oauth.js`, `desktop/main.js`.
- 검증: OAuth Secret 유무 단위 테스트, 관련 JavaScript 문법 검사, ExcelJS 4.4.0 로드·잠금 파일 일치·패키징 경로 사전검사 통과. 최종 설치 EXE의 smoke 검사는 배포 전 계속 진행한다.
- 커밋: `414f6a9`, `c5831f2`

## 현재 미완료 또는 계속 개선할 항목

- 임의 Excel 서식을 셀 병합·수식·인쇄 영역까지 보존하는 자유 출력 틀 편집기
- Google Forms 양식 생성부터 응답 수집까지의 실제 계정 연결 기반 전체 흐름
- GitHub Releases에 설치 파일과 사용자용 변경 내역을 자동으로 함께 게시하는 배포 자동화

## 이후 기록 템플릿

```markdown
### REQ-YYYYMMDD-NN · 짧은 기능명

- 사용자 요청: “요청 핵심 문장”
- 이전 단계: 기존 동작 또는 앞선 요청 ID
- 결정: 이번 변경에서 채택한 동작과 제외한 범위
- 최종 동작: 사용자가 실제로 보게 되는 결과
- 구현: 변경 파일 또는 핵심 구성요소
- 검증: 자동 테스트, smoke, 수동 확인
- 커밋: 해시 또는 `진행 중`
```
