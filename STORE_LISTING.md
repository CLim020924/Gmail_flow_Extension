# Chrome 웹 스토어 등록정보 초안

## 기본 정보

- 이름: Gmail Flow
- 카테고리: 생산성
- 기본 언어: 한국어
- 공개 상태: 미등록(Unlisted)
- 짧은 설명: 사용자 정의 명단과 변수를 활용해 Gmail 초안·발송·예약 작업을 관리합니다.

## 상세 설명

Gmail Flow는 반복적인 Gmail 작성 작업을 한곳에서 관리하는 메일 자동화 도구입니다.

사용자가 직접 명단 컬럼을 만들고 `{컬럼명}` 형식의 변수를 제목과 본문에 넣으면 대상별 내용으로 치환됩니다. Excel, CSV 또는 표 형식 데이터를 붙여넣어 명단을 구성하고, 실제 Gmail에 만들어질 메시지를 대상별로 검토한 뒤 초안 생성, 즉시 발송 또는 예약 발송을 실행할 수 있습니다.

주요 기능:

- 자유로운 명단 컬럼과 개인화 변수
- Excel·CSV·표 데이터 붙여넣기
- 명단, 명단 구조 및 메일 템플릿 저장
- 대상별 Gmail 형식 미리보기
- Gmail 초안 생성, 즉시 발송 및 예약 발송
- 발송 전 2단계 확인과 대상별 검토
- 작업 기록, 오류 확인 및 저장 항목 삭제

## 단일 목적 설명

사용자가 구성한 명단과 개인화 템플릿을 바탕으로 본인의 Gmail 계정에서 메일 초안 작성, 발송, 예약 및 그 작업 기록 관리를 돕습니다.

## 권한 사용 사유

- `storage`: 사용자가 만든 명단, 템플릿, 작성 상태 및 작업 기록을 브라우저에 저장합니다.
- `unlimitedStorage`: 큰 명단과 다수의 템플릿·작업 기록이 일반 저장 용량을 넘더라도 작업 상태를 안정적으로 보존합니다.
- `identity`: 사용자의 명시적인 동작으로 Google OAuth 권한을 요청하고 Gmail API 액세스 토큰을 가져옵니다.
- `identity.email`: 현재 연결된 Chrome/Google 계정을 계정 메뉴에 표시합니다.
- `alarms`: 사용자가 지정한 시각에 예약된 Gmail 발송 작업을 실행합니다.
- `https://gmail.googleapis.com/*`: 사용자가 요청한 Gmail 초안 생성, 발송, 예약 처리 및 메시지 상태 확인을 위해 Gmail API와 통신합니다.

## 심사 담당자 테스트 순서

1. 확장 프로그램을 열고 우측 상단 계정 버튼에서 Gmail을 연결합니다.
2. 추가 기능에서 명단 가져오기를 열고 이메일 컬럼을 포함한 명단을 붙여넣습니다.
3. 메일 작성 화면에서 제목과 본문을 입력하고 명단을 선택합니다.
4. `임시 저장`을 선택한 뒤 확인 절차를 완료합니다.
5. Gmail 임시보관함에서 생성된 초안을 확인합니다.

## 등록 전 교체할 값

- 개인정보처리방침 URL: `https://github.com/CLim020924/Gmail_flow_Extension/blob/main/PRIVACY.md`
- 웹 스토어 항목 ID가 생성된 뒤 그 ID에 연결된 OAuth 클라이언트 ID로 `manifest.json`을 갱신해야 합니다.
